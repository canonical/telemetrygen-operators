# Charm runtime dependencies. Pinned loosely so that charmcraft can resolve
# a compatible wheel for the build platform.
ops>=3.1,<3.2
pydantic>=2,<3
cosl>=1.0
# The unified OTLP relation library. Provides:
#   charmlibs.interfaces.otlp.OtlpRequirer
# which encapsulates the modern `otlp` relation interface (one relation
# carrying traces+metrics+logs over gRPC or HTTP), superseding the legacy
# loki_push_api / prometheus_scrape / prometheus_remote_write trio.
charmlibs-interfaces-otlp>=0.5,<0.6
